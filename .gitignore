# dependencies
node_modules/

# next.js
/.next/
/.next.stale.*/
/out/

# production
/build

# misc
.DS_Store
*.pem

# LibreOffice-Locks & Temp-Dateien
.~lock.*
*.tmp

# debug
npm-debug.log*

# env files (NEVER commit secrets!)
.env
.env.*
.env*.local
# Exception: template with placeholders (no real secrets)
!.env.example

# Playwright / E2E
/test-results/
/playwright-report/
/playwright/.cache/

# Firebase service account keys (NEVER commit!)
*firebase-adminsdk*.json
*service-account*.json

# Personal/sensitive files
ausweis_*.jpg
fuehrerschein*.pdf
# Ausgefüllte Behörden-Anträge mit personenbezogenen Daten
/Bayern-45a-Antrag/
*.aab
*.apk
*.mobileprovision
*.lock.*.xlsx#

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# temp commit message helper (osascript workaround)
.commit_msg_tmp

# supabase
supabase/.temp/

# private targeting strategy (nicht öffentlich)
.TARGETING-STRATEGY-PRIVATE.md
*/.TARGETING-STRATEGY-PRIVATE.md
*.mp4

# Archive: privater/PII-Bereich (Ausweise, Führerscheine, Release-Builds)
/archive/private/

# Archive: alte Build-Caches (große Binaries, lokal halten)
/archive/next-old/
/archive/video-generation/
.tmp_organizer.png
xcode-state.png

# graphify knowledge graph (lokal, wird bei Bedarf neu generiert)
graphify-out/
.github/workflows/deploy-chairmatch.yml
sim-screenshot.png

# Lokale Maschinen-Analyse (gehört nicht ins Repo)
MAC_SPEICHER_ANALYSE.md

# deploy.sh IndexNow-Ping-Log
.indexnow-ping.log
marketing/.footer-preview-tmp.png

# Temp-Previews aus Bild-Editing (Loeschung auf Mount blockiert)
marketing/_tmp*.png

# Lokale Shadow-DB (scripts/shadow-db.sh) — kompletter Postgres-Cluster, nie committen
.shadow-db/

# Next.js 16 uses proxy.ts — middleware.ts must not exist
middleware.ts
.next-old/
